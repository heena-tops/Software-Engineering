#include<stdio.h>

int main()
{
	/*
		Recursion : To call Itself again and again is called as 
					recursion
					
					
		Recursive Function : A Funciton that call itself
							is called rercursive function
					
		fact(5) = 5*fact(4)
		fcat(4) = 4*fact(3)
		fact(3) = 3*fact(2)
		fact(2) = 2*fact(1)
		fact(1) = 1*fact(0)
		fcat(0) = 1		
	
	*/
	
	return 0;
}
